# SKILL.md delta (apply when curator load-gate allows SKILL.md patch)

If SKILL.md still says “poll --status pending” / only UI click pitfall:

Replace **Agent poll & complete** + **UI pitfalls** with:

- Progress CLI (`--status open`, `--progress`, `--done --summary`, `--fail`)
- Agent-first mandatory section
- Tabs UI + Agent activity + migrations 0002+0003
- Pointers: `agent-progress.md`, `tabs-ui.md`, `agent-first-options.md`, `d1-api.md`

Also bump description to mention progress/certainty if desired.
