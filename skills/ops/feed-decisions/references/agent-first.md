# Agent-first before HITL

Cooper is not the researcher.

1. Readonly investigation first: gog headers/body, official bookmarks (never untrusted email links), SA `op --vault cm|cooper|dev`, Studio CUA/Kernel as needed.
2. Do not ask “what did the console show?” — log in and look.
3. Post findings in the HITL `body` (and optional kanban comment if a long repair is tracked elsewhere).
4. Open HITL only for a true gate: pay/don't-pay, approve/reject send, security/MFA, missing SA secret (ask move that one title into vault `cm`).
5. After Cooper answers, execute under the right skill (`financial-operations`, etc.) and update progress via `poll-responses.ts`.
