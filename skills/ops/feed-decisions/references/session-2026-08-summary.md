# Session summary — feed decisions stack (2026-08)

Canonical pointers for this session’s product:

| Topic | File |
|---|---|
| Progress lifecycle + CLI | `agent-progress.md` |
| D1 API + statuses running/failed | `d1-api.md` |
| Tabs UI / no stacked inputs | `tabs-ui.md` |
| Agent-first option labels | `agent-first-options.md` |
| SKILL.md patch backlog | `SKILL-pointer-progress.md` |

## Hard preferences (Cooper)

1. No clipboard “Copy for Hermes”
2. No human-research prompts
3. Visible agent progress on feed
4. Pay decisions as feed checkboxes, few at a time

## Prod paths

`~/git/darkmatter/feed` — Worker + D1 migrations 0002/0003 + `agent-activity.tsx`
